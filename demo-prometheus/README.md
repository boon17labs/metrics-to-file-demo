# demo-prometheus

Placeholder module for exercising `metrics-to-file-prometheus` once that
module exists (see `metrics-to-file`'s own README — it is currently "not
started"). Add a dependency on `io.github.boon17labs:metrics-to-file-prometheus`
and a demo app here when that module ships.
