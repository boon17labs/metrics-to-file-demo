# demo-autoinstrument

Placeholder module for exercising `metrics-to-file-autoinstrument` once that
module exists (see `metrics-to-file`'s own README — it is currently "not
started"). Add a dependency on `io.github.boon17labs:metrics-to-file-autoinstrument`
and a demo app here when that module ships.
