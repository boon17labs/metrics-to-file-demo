# demo-spring

Placeholder module for exercising `metrics-to-file-spring` once that module
exists (see `metrics-to-file`'s own README — it is currently "not started").
Add a dependency on `io.github.boon17labs:metrics-to-file-spring` and a
Spring Boot demo app here when that module ships.
